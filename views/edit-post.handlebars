
<article>
  <a href="/dashboard"> &larr; Back to dashboard</a>
  <h2 class="align-content-center">
    Edit Post
  </h2>
  <form class="edit-post-form p-2 my-2 mx-2">
    <div class="p-2 my-2 mx-2">
      <input name="post-title" type="text" value="{{post.title}}" />
      <span>({{format_url post.post_url}})</span>
    </div>
    <div class="p-2 my-2 mx-2">
      {{post.vote_count}} {{format_plural "point" post.vote_count}} by you on {{format_date post.created_at}}
      |
      <a href="/post/{{post.id}}">{{post.comments.length}} {{format_plural "comment" post.comments.length}}</a>
    </div>
    <button type="submit" class="btn btn-outline-dark">Save post</button>
    <button type="button" class="btn delete-post-btn btn-outline-dark">Delete post</button>
  </form>
</article>

<form class="comment-form p-2 my-2 mx-2">
  <div>
    <textarea name="comment-body"></textarea>
  </div>

  <div class="p-2 my-2 mx-2">
    <button type="submit" class="btn btn-outline-dark">Add comment</button>
  </div>
</form>

{{> comments post.comments}}

<script src="/javascript/edit-post.js"></script>
<script src="/javascript/delete-post.js"></script>
<script src="/javascript/comment.js"></script>